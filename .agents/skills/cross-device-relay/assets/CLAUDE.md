@AGENTS.md

# Claude Code adapter

- Treat `AGENTS.md` as the shared authority for this checkout.
- Claude private memory and hooks are advisory and must not contain facts required by another agent.
- Use the same checkout and `.relay/current.md` for an active relay task; do not create a parallel implementation or automatic worktree.
- Before handing off, write every continuation fact to Git, durable project documentation, or the bounded relay ledger.

